/** Stub — replaced by a parallel Phase 5 task. Final file name/signature only. */
export function AppFooter(props: { onOpenAbout: () => void }) {
  void props.onOpenAbout;
  return <section data-testid="stub-app-footer" />;
}
