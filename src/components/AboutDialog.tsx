/** Stub — replaced by a parallel Phase 5 task. Final file name/signature only. */
export function AboutDialog(props: { open: boolean; onClose: () => void }) {
  void props.open;
  void props.onClose;
  return <section data-testid="stub-about-dialog" />;
}
